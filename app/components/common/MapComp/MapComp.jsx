import React from 'react';
let {Component} = React;
import styles from './MapComp.css';

export default class Map extends Component {
  render() {
    return (
      <div className={styles.map}>
      </div>
    );
  }
}
